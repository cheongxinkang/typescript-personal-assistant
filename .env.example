# Copy to .env and fill in real values. This file is checked into git and
# must never contain a real secret.

# Discord bot token, from the Developer Portal's Bot tab.
# Requires the "Message Content" privileged intent enabled — see
# docs/running-locally.md.
DISCORD_BOT_TOKEN=

# The Discord channel ID the bot listens on. Messages in any other channel
# are ignored. Enable Developer Mode in Discord to copy a channel ID.
DISCORD_CHANNEL_ID=

# Anthropic API key, from console.anthropic.com.
ANTHROPIC_API_KEY=

# Postgres connection string. Change the port if 5432 is already taken by
# another project's Postgres on this machine.
DATABASE_URL=postgres://postgres:postgres@localhost:5432/personal_assistant

# IANA timezone used to resolve date expressions like "tomorrow 7pm",
# e.g. Asia/Singapore, America/New_York, Europe/London.
OWNER_TIMEZONE=

# Basic Auth credentials gating the read-only DB-visibility viewer
# (phase_2a-db-visibility.md). Generate with `openssl rand -hex 16` —
# never a memorable phrase, since this is on a public domain.
BASIC_AUTH_USER=
BASIC_AUTH_PASSWORD=

# Port the viewer listens on, separate from the main app's PORT. Defaults
# to 8080. This is the only port published to the host (hostPort, loopback
# only) for the Cloudflare Tunnel to reach.
VIEWER_PORT=
